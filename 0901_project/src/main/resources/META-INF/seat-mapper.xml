<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="SeatMapper">

	<select id="seat" parameterType="java.lang.String" resultType="kr.co.ictedu.dto.ReserveSeatDto">
		select reserve_date, theater_no, play_time, seat_cnt
		from theater
		where reserve_date = #{seatreserve_date}
	</select>
	
	<insert id="order" parameterType="kr.co.ictedu.dto.ReserveDto">
		insert into order_main(id, reserveSequence, title, runningTime, movieDate, ticketNumber, selectedSeat, selectTheater, selectWhere, movieAge, reserve_date, seat_cnt, pay)
		VALUES ( #{id}, #{reserveSequence}, #{title}, #{runningTime}, #{movieDate}, #{ticketNumber}, #{selectedSeat}, #{selectedTheater},#{reserveWhere}, #{movieAge}, now(), #{seat_Cnt}, #{pay})
	</insert>
	
	<select id="seatcnt" parameterType="kr.co.ictedu.dto.ReserveDto" resultType="kr.co.ictedu.dto.ReserveSeatDto">
		select seat_cnt, selectedSeat
		from order_main
		where selectWhere = #{reserveWhere}
		and runningTime = #{runningTime}
		and movieDate = #{movieDate}
		order by order_no desc
		limit 1
	</select>

	<select id="selectedseat" parameterType="kr.co.ictedu.dto.ReserveDto" resultType="kr.co.ictedu.dto.SelectedDto">
		select selectedSeat
		from order_main
		where selectWhere = #{reserveWhere}
		and runningTime = #{runningTime}
		and movieDate = #{movieDate}
	</select>


</mapper>
