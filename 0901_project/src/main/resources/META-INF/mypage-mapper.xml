<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MyPageMapper">
	
	<delete id="memDelete" parameterType="kr.co.ictedu.util.dto.MemberDTO">
	delete from member
	where mno = #{mno}
	</delete>
	
	<select id="infoSelect" parameterType="java.lang.String" resultType="kr.co.ictedu.util.dto.MemberDTO">
	select mno, mid, mname, mpwd, post_code, maddr1, maddr2, mtel
	from member
	where mno = #{mno}
	</select>
	
	<update id="infoUpdate" parameterType="kr.co.ictedu.util.dto.MemberDTO">
	update member set mpwd = #{mpwd}
					, mtel = #{mtel}
	where mno = #{mno}
	</update>
	
	<select id="orderList" parameterType="java.lang.String" resultType="kr.co.ictedu.dto.CGVReserveDto">
	select m.mno, o.order_no, o.id, o.reserveSequence, o.title, o.runningTime, o.movieDate, o.ticketNumber
	, o.selectedSeat, o.selectTheater, o.movieAge, o.reserve_date
	from order_main o, member m
	where o.id = m.mid 
	and m.mno = #{mno}
	order by order_no desc
	</select>
	
	<select id="recentOrder" parameterType="java.lang.String" resultType="kr.co.ictedu.dto.CGVReserveDto">
	select m.mno, o.order_no, o.id, o.reserveSequence, o.title, o.runningTime, o.movieDate, o.ticketNumber
	, o.selectedSeat, o.selectTheater, o.movieAge, o.reserve_date
	from order_main o, member m
	where o.id = m.mid 
	and m.mno = #{mno}
	order by order_no desc
	limit 5;
	</select>
	
</mapper>
