<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="JoinMapper">

	<insert id="join" parameterType="kr.co.ictedu.util.dto.MemberDTO">
	INSERT INTO member ( mid, mpwd, mname, mtel, memail, mdate, maddr1, maddr2, post_code )
	VALUES ( #{mid}, #{mpwd}, #{mname}, #{mtel}, #{memail}, now(), #{maddr1}, #{maddr2}, #{post_code} )
	</insert>

	<select id="idCheck" parameterType="java.lang.String" resultType="int">
	select count(mid) cnt
	from member
	where mid = #{mid}
	</select>
	
	<select id="emailValidate" parameterType="java.lang.String" resultType="int">
	select count(memail) cnt
	from member
	where memail = #{memail}
	</select>

</mapper>
